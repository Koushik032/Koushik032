#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl '\n';

int main()
{
    string v;
    int n;
    cin >> n;

    cin >> v;

    int ln=v.size();

    cout << v[ln-1] << endl;

    return 0;
}
